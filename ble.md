[hrf](hrf.md)  

# [SideChannel](./files/ble/sidechannel.md)  

# DTM MT8852B
========================================================  
【一、PDF 第 2.10.8 节表格原文指令 (截图原样)】  
（批注：表格把 01 00 标为“接收测试”与规范位解析不符——
 01 00 高2位=00，按位解析是 Setup 命令#1（测试长度高位=0），
 既不是复位（00 00）也不是接收测试（40 xx）。
 表格回复 10 00 称“命令错误”也有误：按 Silabs dtm.c 与 Core Spec，
 错误回复是 00 01）  
========================================================  

// 1. 原文“接收测试”指令（不推荐用，按位解析其实是 Setup 命令#1）
TX: 01 00
RX: 00 00   （命令接收成功——Setup 命令的固定回包，恰好印证它属 Setup 类）
RX: 00 01   （命令错误；原文写 10 00 有误）

// 2. 原文“测试结束”指令 (无测试运行时返回错误)
TX: C0 00
RX: 00 01   （命令错误；原文写 10 00 有误。
            注：Silabs EFR32 固件对“无测试时结束”不报错，回包计数 80 00）


========================================================  
【二、标准指令 (按 2.10.5 节位定义，已修正)】  
（帧格式：2字节连发，字节间隔须<5ms  
  字节1 = [命令类型2bit]<<6 | control/frequency[6bit]  
  命令类型：00=Setup  01=接收  10=发射  11=结束  
  字节2 = [参数/长度]<<2 | 包类型[2bit]  
  包类型：00=PRBS9  01=11110000  10=10101010  11=11111111）  
（频率=通道0 (2402MHz)，数据长度=37字节，包类型=PRBS9）  
========================================================  

// 1. 复位命令 (LE_Reset)：结束当前测试并恢复默认设置
TX: 00 00
RX: 00 00   (成功)   /   00 01   (失败)

// 2. 接收测试 (LE_Receiver_Test) —— DUT开始监听空中包
TX: 40 94
RX: 00 00   (成功启动)   /   00 01   (失败启动)

// 3. 发射测试 (LE_Transmitter_Test) —— DUT开始发射信号
TX: 80 94
RX: 00 00   (成功启动)   /   00 01   (失败启动)

// 4. 结束测试 (LE_Test_End) —— 停止当前测试并上报结果
TX: C0 00
RX: 不分接收/发射测试，一律回 2 字节包计数（EV位=1）：
    16位字 = 0x8000 | 计数值，高字节 = 0x80|计数高7位，低字节 = 计数低8位
    例：收到 5 包 -> 80 05；发射测试结束（Silabs 固件）-> 80 00


========================================================  
【附：标准指令的二进制拆解 (方便你排查)】  
========================================================  
TX: 40 94
  字节1 0x40 = 0b0100 0000 -> CMD=01(接收), FREQ=000000(通道0=2402MHz)
  字节2 0x94 = 0b1001 0100 -> 高6位=100101(=37字节), 低2位=00(PRBS9)

TX: 80 94
  字节1 0x80 = 0b1000 0000 -> CMD=10(发射), FREQ=000000(通道0)
  字节2 0x94 -> LEN=37, PKT=PRBS9
  （旧版写 80 25 是错的：0x25=0b0010 0101 -> LEN=9, PKT=01(11110000)）

TX: C0 00
  字节1 0xC0 = 0b1100 0000 -> CMD=11(结束), FREQ 无意义
  字节2 0x00 -> 无参数

（长度>63字节：先发 Setup 命令#1 设长度高位 TX 01 xx（xx高6位=高位值），
 如 255字节 = 01 40，再发 80 FF(LEN=63)）


========================================================    
【三、设置发射功率 (Transmitter Power Level，Core Spec Vol6 PartF 标准 Setup 命令)】    
（注意：与其它 Setup 参数不同，功率是“带符号dBm 原样字节”，不做 <<2 编码！    
  0x7F=请求最大支持功率，0x7E=请求最小支持功率    
  功率命令只影响之后启动的发射测试，发射启动时固件会重新套用）    
========================================================    

TX: 09 00      -> 0 dBm
TX: 09 0A      -> +10 dBm
TX: 09 13      -> +19 dBm  
TX: 09 7F      -> 请求最大功率（回包 bit9 置位表示“已设为最大”）  
TX: 09 7E      -> 请求最小功率（回包 bit8 置位表示“已设为最小”）  

回包格式：高字节 = dBm>>7，低字节 = ((dBm&0x7F)<<1) | status  
超出芯片/配置支持范围 -> 00 01（错误，功率不变）  

本工程 (EFR32MG24A410) 限制：  
- config/sl_bluetooth_config.h 的 SL_BT_CONFIG_MAX_TX_POWER=80（0.1dBm=8dBm）  
  -> 09 0A 会被拒（00 01），需改 >=100 并重编  
- A410 芯片射频最大 +10 dBm，09 13（+19dBm）物理上无法达成，  
  需换 20dBm 变体（MG24 A420/B320）  


========================================================  
【四、MT8852B 能否发 09 改功率】  
========================================================  
1. 09 是蓝牙 Core Spec 标准 Setup 命令（非 Silabs 私有），任何按规范
   实现的 DUT 都应支持。
2. MT8852B 自动测试脚本（Option 26 BLE）传统上只发 00/40xx/80xx/C0
   四条，测的是 DUT 自己配置的功率；老固件大概率不带 09。
   是否支持取决于固件版本——实测判别：向 DUT 发 09 0A，
   回 00 14 = 生效，回 00 01 = 拒绝。
3. 不依赖仪器固件的可靠做法：
   a. MT8852B 的 DUT Control 手动模式逐条键入 16 进制命令 09 XX；
   b. 或 PC+USB转UART 直接接 DUT 发 09 XX（DTM 就是普通 UART，
      可与仪器分线共用；先设功率，再启动发射测试）。
